# AIRO · 智能音乐推荐系统
## 演示文稿内容（约 10 分钟 / 15 页）

> 本文档为 WPS AI 生成 PPT 的内容源。每页包含：**标题 / 要点 / 讲稿提示**。
> 建议视觉风格：深色科技蓝（`#0F1C2E` 底 / `#A8DADC` 青蓝 / `#E63946` 强调红），与系统 UI 保持一致。

---

## 第 1 页 · 封面

**AIRO · 智能音乐推荐系统**

基于向量检索与大语言模型的个性化音乐推荐

- 副标题：三条推荐路径 · 用户画像驱动 · 可解释推荐
- 汇报人 / 日期：___________

> **讲稿**：大家好，今天演示的是 AIRO —— 一个智能音乐推荐系统。它的核心特点是：不是用一个模型解决所有问题，而是针对用户三种不同的听歌心理，设计了三条各自独立的推荐路径，并让它们共享同一个用户画像。

---

## 第 2 页 · 问题定义：用户找歌的三种心理

**同一个"推荐"，背后是三种完全不同的需求**

| 场景 | 用户心理 | 典型表达 |
|---|---|---|
| **A. 有明确锚点** | "我喜欢这首，给我类似的" | "放一首像 Shape of You 的歌" |
| **B. 有模糊意图** | "我说不清要什么，但我知道现在想听什么感觉" | "适合下雨天听的"、"深夜放松的爵士" |
| **C. 无表达欲** | "别问我，直接给我" | 打开 App，直接点播放 |

**关键洞察**：把这三类需求塞进一个推荐模型，会导致意图冲突、参数打架、体验割裂。

> **讲稿**：这是我们的出发点。传统推荐系统通常只有一条链路。但观察真实用户行为会发现，用户找歌时的心理状态差异极大 —— 有时他手里有明确的锚点歌，有时只有一个模糊的感觉，有时干脆什么都不想表达。我们选择为这三种状态分别设计路径，而不是强行统一。

---

## 第 3 页 · 系统总览：四层架构

```
┌──────────────────────────────────────────────────────────┐
│  接入层    Django 6.1 · 会话登录 · 单页三 Tab 交互        │
├──────────────────────────────────────────────────────────┤
│  推荐层    路线A 种子相似 │ 路线B 意图理解 │ 路线C 画像直推 │
├──────────────────────────────────────────────────────────┤
│  画像层    profile_service（唯一读写入口 · 三路线共享）    │
│            记忆衰减 · 流派索引化 · 口味向量化             │
├──────────────────────────────────────────────────────────┤
│  数据层    Qdrant 向量库 · SQLite 业务库 · 本地 LLM       │
└──────────────────────────────────────────────────────────┘
```

**设计原则**：画像与推荐解耦 —— 三条路线平等消费同一份画像，画像的演进不影响任一推荐逻辑。

> **讲稿**：整个系统分四层。最值得强调的是中间这层"画像层" —— 这是我们重构时做的核心决策：把用户画像从"推荐算法的附属品"提升为"独立的共享资产"。三条推荐路线都通过同一个 profile_service 读写画像，谁都不能绕过它自己解析数据。这样画像的升级（比如加入时间衰减）只需要改一个地方。

---

## 第 4 页 · 数据资产

**1238 万首歌曲的声学向量库**

| 资产 | 规模 | 说明 |
|---|---|---|
| **Qdrant `spotify_tracks`** | **12,382,652** 条 | 64 维声学向量（Embeat 模型），COSINE 距离 |
| **Qdrant `tag_index`** | **6,277** 个标签 | 512 维语义向量（bge-small-zh-v1.5） |
| **流派映射表** | **6,292** 条 | `genre_idx → genre_name`（everynoise 体系） |
| **标签语义库** | 2.7 MB | 每标签含中文释义 + 用户可能说法 |
| **业务库** | SQLite | 用户档案、反馈记录 |

**数据工程考量**
- 向量使用 **uint8 量化 + on_disk 存储**，在 12M 规模下控制内存占用
- 已建 payload 索引：`track_name` / `artist_name` / `artist_genre_idx` / `artist_idx` / `popularity`

> **讲稿**：数据底座是 1238 万首 Spotify 歌曲，每首有一个 64 维的声学向量 —— 它是由音频特征（舞曲性、能量、情绪、节奏等）编码而成的"听感指纹"。另外我们还有一层 6277 个音乐标签的语义索引，标签用中文描述，比如"深夜放松的爵士"可以匹配到 dinner jazz、deep vocal jazz 这些专业标签。这一层是路线 B 的基础。

---

## 第 5 页 · 路线 A：种子相似推荐

**以一首歌为锚点，找声学上最接近的歌**

**处理流程**
1. **种子定位** → `track_id` / `ISRC` / `歌名+歌手` 三级匹配，多条结果按「去混音 → 最热门 → 名最短」排序
2. **声学召回** → 用种子向量做 ANN 检索，召回 `top_k × 10`（上限 512）条
3. **流派过滤** → 同 `artist_genre_idx` 硬过滤；无交集直接淘汰
4. **混合打分** → `最终分 = 向量相似度 + 流派加成`
   - 流派索引完全相同：**+0.15**
   - 否则按流派交集数 **每项 +0.03**（上限 3 项）
5. **去重与约束** → 按歌名去重；同歌手上限 `top_k × 15%`

**实测**：种子 *Shape of You*，响应 1.4 秒，Top 结果相似度 0.997

> **讲稿**：路线 A 解决的是"给我相似的"这类需求。它的核心不只是向量检索 —— 纯向量检索的问题是容易跨风格漂移，比如你要一首流行歌，它可能给你一首听起来"数值上接近"但其实完全不同的类型。所以我们加了流派硬过滤和流派加成，把同流派的结果往前排。同时限制同一歌手最多出现 15%，避免整页都是一张专辑。

---

## 第 6 页 · 路线 B：自然语言意图推荐

**把"我说不清"变成"系统能懂"**

**四阶段流水线**

```
用户输入「深夜放松的爵士」
   ↓ ① 多路标签召回
关键词表命中 (jazz 2.0 / relaxative 2.0)  ⊕  向量语义召回 15 条
   ↓ ② LLM 选标签（约束：不选过宽大类、不选地域无关）
['jazz', 'relaxative', 'smooth jazz', 'chill out', 'chill lounge']
   ↓ ③ 标签过滤歌曲（OR 匹配 + 命中数 + 流行度排序）
候选 20 首
   ↓ ④ 画像融合重排 + 多样性约束
返回结果
```

**双路召回的意义**：关键词表覆盖"印度""韩流"这类向量易漂移的词；向量召回兜底口语化表达。

**实测**：端到端约 10 秒（含 LLM 推理 2.8 秒），20 首结果全部为 smooth jazz / chill lounge 类

> **讲稿**：路线 B 是最有意思的一条。用户说"深夜放松的爵士"，系统需要做两件事：先理解意图，再找歌。我们用了一个"标签中间层" —— 先把用户的自然语言映射到 6277 个专业音乐标签上，再用标签去过滤歌曲。这样做的价值是把 1238 万首的搜索空间压缩到几百个标签的语义空间。理解意图这一步交给本地大模型，我们给了明确的约束，比如不要选 pop、rock 这种太宽泛的大类。

---

## 第 7 页 · 路线 C：画像直推（每日推荐）

**零输入 · 每次不同 · 冷启动可用**

**三路召回 → RRF 融合 → MMR 重排**

| 通道 | 机制 | 召回量 |
|---|---|---|
| ① **流派索引召回** | `artist_genre_idx ∈ 画像流派`（走整数索引，毫秒级） | 300 |
| ② **口味向量召回** | 画像质心做 ANN 检索，捕捉标签外的听感特征 | 300 |
| ③ **歌手扩散召回** | 喜欢的歌手 → `related_artist_idxs` → 发现新歌手 | 200 |

**两个关键算法**
- **RRF（Reciprocal Rank Fusion）**：只用排名不用分数，天然免疫三路评分量纲不可比的问题
- **MMR（Maximal Marginal Relevance）**：在"相关性"与"与已选项的差异度"间权衡，避免同歌手/同流派刷屏

**"每次不同"的实现**：偏置随机取样 —— 高相关段占 70% 名额（保证不跑偏），长尾段占 30%（提供探索性）

> **讲稿**：路线 C 是最"像产品"的功能 —— 用户什么都不用输入，点一下就给一批推荐，而且每次点都是新的一批。它的技术难点在于：三路召回的分数量纲完全不可比，一个按流行度排、一个按余弦相似度排。我们没有去硬调权重，而是用了 RRF 这个业界标准做法，只看排名不看分数。另外为了保证结果多样，用了 MMR 算法，同时限制同一歌手最多 2 首。

---

## 第 8 页 · 用户画像：三条路线的共享底座

**从"推荐算法的附属品"升级为"一等公民"**

| 能力 | 实现 |
|---|---|
| **记忆衰减** | 30 天半衰期 —— 旧偏好权重自然衰减，避免三个月前的口味绑架今天 |
| **流派索引化** | 反馈文本 → `artist_genre_idx`，使画像能直接驱动 Qdrant 索引查询 |
| **口味向量化** | 偏好流派的代表性歌曲向量加权平均 → 64 维"口味质心" |
| **冷启动分档** | `cold`（无反馈）/ `warm`（1-4 条）/ `hot`（≥5 条），三档走不同策略 |
| **失败记账** | LLM 摘要失败也记录时间戳，6 小时后才允许重试，避免无限重试 |

**画像结构（部分）**
```json
{
  "schema_version": 2,
  "stage": "hot",
  "preferred_genres": [{"idx": 1, "name": "pop", "weight": 3.0}],
  "preferred_artists": [{"name": "Ed Sheeran", "weight": 2.0}],
  "liked_artist_idxs": [45, 158, 395],
  "query_text": "pop, uk pop, dance pop"
}
```

> **讲稿**：这页是系统的核心创新点。用户画像在大多数系统里只是推荐算法的内部参数，我们把它独立出来了。举两个例子：第一是记忆衰减，用户三个月前喜欢的东西不该和昨天喜欢的东西等权，我们用 30 天半衰期做衰减。第二是流派索引化 —— 用户反馈里存的是文本流派名，我们把它映射成 Qdrant 的整数索引，这样画像可以直接驱动数据库查询，而不是做全表扫描。

---

## 第 9 页 · 反馈闭环：越用越准

```
用户点击「喜欢 / 不喜欢」
      ↓
POST /api/feedback/  →  写入反馈记录（含流派索引）
      ↓
rebuild_profile()  →  纯统计 + 时间衰减（毫秒级，不调 LLM）
      ↓
画像更新  →  下一次三条推荐路线自动生效
```

**关键工程决策：反馈接口不调用 LLM**

早期版本在反馈写入时同步调用大模型生成画像摘要，导致**一次点赞要等一次模型推理**。重构后改为：
- 反馈接口只做统计（**毫秒级**）
- 画像摘要改为**按需生成**（用户主动点按钮，约 10-30 秒）

**实测**：反馈接口响应时间从「一次模型推理」降到 **毫秒级**

> **讲稿**：推荐系统要越用越准，所以反馈闭环很重要。这里有一个我们踩过的坑值得分享：最初的设计里，用户点一次"喜欢"，后端就同步调用大模型重新分析画像。结果就是用户点个赞要等好几秒。后来我们把这个解耦了 —— 反馈只做纯统计，毫秒级返回；需要模型的地方改成按需触发。

---

## 第 10 页 · 可解释性：让推荐"有理由"

**每条推荐都附带一段由 LLM 生成的中文解释**

**针对三种来源，设计三套不同的提示词**

| 模式 | 适用场景 | 提示词核心 |
|---|---|---|
| `seed` | 歌名找相似 | 对比两首歌的**共同音乐特征**（流派/编曲/节奏/情绪） |
| `profile` | 每日推荐 | 解释这首歌**如何契合用户口味**（无种子歌） |
| `intent` | 描述找歌 | 解释这首歌**如何匹配用户描述**（无种子歌） |

**实测输出（`profile` 模式）**
> "《Perfect》的编曲以温暖的钢琴旋律开场，搭配轻柔的弦乐铺陈，整体氛围干净而细腻，非常契合 **uk pop 注重情感表达和细腻旋律**的特点……"

**降级设计**：模型不可用时，返回基于共同流派的机械兜底文案，避免界面出现空白

> **讲稿**：推荐结果如果只说"猜你喜欢"是没有说服力的。所以每条推荐我们都生成一段中文解释。这里有个细节：每日推荐和描述找歌这两条路线**没有种子歌曲**，如果沿用"两首歌为什么相似"的提示词，模型就会凭空编造。所以我们为三种来源设计了三套提示词。另外我们做了降级兜底，模型挂了也不会让用户看到空白。

---

## 第 11 页 · 技术栈

| 层次 | 技术选型 | 版本 | 用途 |
|---|---|---|---|
| **Web 框架** | Django | 6.1 | 路由、模板、会话、ORM |
| **向量数据库** | Qdrant | 1.19 (client) | 12M 级 ANN 检索、payload 过滤 |
| **文本嵌入** | sentence-transformers + bge-small-zh-v1.5 | 6.0.1 | 中文标签语义向量（512 维） |
| **深度学习** | PyTorch | 2.13 (CPU) | 模型推理后端 |
| **本地大模型** | Ollama + Qwen3-4B-Instruct | 4B / Q4_K_M | 意图理解、标签选择、理由生成 |
| **数据处理** | Polars / PyArrow / pandas | 1.44 / 25.0 / 3.0 | 大规模 Parquet 数据管道 |
| **协同过滤** | implicit (ALS) | 0.7.3 | 离线实验 |
| **前端** | 原生 HTML/CSS/JS + Canvas | — | 单页三 Tab、粒子装饰层 |

**外部集成**：MCP 协议接入网易云音乐（Meting-Agent），补充播放链接

> **讲稿**：技术栈上我们坚持"够用且可控"。向量检索用 Qdrant，这是目前最成熟的向量数据库之一，支持 uint8 量化和磁盘存储，让 1200 万向量能在普通机器上跑。大模型选择本地部署的 Qwen3-4B，通过 Ollama 管理 —— 一个 4B 的量化模型，2.5GB，在消费级显卡上就能跑，而且数据不出本地。

---

## 第 12 页 · 工程实践：性能优化

**从"页面卡 3 秒"到"6 毫秒"的优化历程**

| 问题 | 根因 | 优化手段 | 效果 |
|---|---|---|---|
| 反馈页加载慢 | LLM 探测同步阻塞 | 改为**后台线程 + 30s 缓存** | 3058ms → **6ms** |
| LLM 不可用时全站卡死 | 3 次重试 × 120s 超时 | **连接错误立即失败** + 冷却期 | 362s → **2s** |
| 画像重建慢 | 每次解析 2.8MB JSON | **预生成精简缓存**（134KB） | 876ms → **6ms** |
| 摘要反复重试 | 失败不记账 | **失败写入时间戳**，6h 后才重试 | 消除死循环 |
| 无效查询 | `track_id` 无索引 | 改为**走已索引字段** + Python 侧过滤 | 超时 → 毫秒级 |

**方法论**：先逐层计时定位瓶颈，再针对性优化 —— 而不是凭直觉猜测

> **讲稿**：这页想分享一个真实的性能优化案例。用户反馈"反馈页一直加载中"。我们没有直接猜原因，而是写了逐层计时的脚本，结果发现真正的瓶颈有四个，而且其中一个是我自己在上一轮优化时引入的 —— 为了判断模型是否可用，加了一个同步探测，反而多花了 2 秒。定位到之后，反馈页从 3 秒降到 6 毫秒。这个经验是：一定要用数据定位问题，不要凭直觉。

---

## 第 13 页 · 工程质量：踩过的坑与修复

| 问题 | 现象 | 根因 | 修复 |
|---|---|---|---|
| **索引缺失** | 按 `track_id` 查询超时 >30s | 该字段**没有 payload 索引**，12M 点全表扫描 | 改用已索引字段；必要时 Python 侧过滤 |
| **上下文溢出** | 推荐理由返回空 | 长文本塞进 `num_ctx=2048` 限制 | 移除该数据源；加固长度预算 |
| **启动卡死** | Django 起不来 | `sentence_transformers` 顶层 import 连带加载 torch，内存不足崩溃 | 改为**函数内懒加载** |
| **BLAS 内存爆炸** | `OpenBLAS error: Memory allocation failed` | OpenBLAS 按核数开线程池，内存不足 | 限制线程数为 1（`runtime_env.py`） |
| **编码崩溃** | `UnicodeEncodeError: 'gbk'` | Windows 控制台 GBK 打印 emoji | 清理 emoji + 设 `PYTHONIOENCODING` |

**沉淀**：`runtime_env.py` 统一管理运行时环境变量，在 `manage.py` / `wsgi.py` / `asgi.py` 最顶部导入

> **讲稿**：这页是两个真实的排障案例。第一个是索引问题 —— 我们想按歌曲 ID 精确查一条记录，结果 30 秒超时。排查后发现这个字段在 Qdrant 里**没有建索引**，所以每次查询都在 1200 万个点上做全表扫描。第二个是内存问题 —— 一个第三方库在导入时会按 CPU 核数创建线程池，在内存紧张时直接崩溃。这些都不是算法问题，但会直接决定系统能不能跑起来。

---

## 第 14 页 · 系统演示要点

**演示脚本建议（按顺序展示）**

1. **登录** → 展示全站登录中间件、用户隔离
2. **路线 A**：输入 *Shape of You* → 展示候选下拉选择 → 相似结果 + 匹配度 + 理由
3. **路线 B**：输入"深夜放松的爵士" → 展示系统理解的标签 → 结果 + 理由
4. **路线 C**：点「为我推荐」→ **连续点两次，展示结果不同** → 冷启动提示
5. **反馈闭环**：点「喜欢」→ 展示反馈状态圆点 → 再打开每日推荐看变化
6. **我的反馈页**：展示客观统计（流派分布 / 歌手分布）+ 按需生成 LLM 摘要
7. **数据浏览页**：展示 1238 万首数据的检索能力

**视觉亮点**：鼠标移动时的粒子吸引效果（Canvas 装饰层，`pointer-events: none`，与业务完全解耦）

> **讲稿**：演示时建议大家重点关注两点：第一是连续点两次每日推荐，结果会不同，但仍然在用户口味范围内；第二是点完"喜欢"之后，推荐结果会随之变化，这是反馈闭环在起作用。

---

## 第 15 页 · 总结与展望

**核心成果**

- 针对三种用户心理，设计**三条独立且共享画像**的推荐路径
- 构建 **1238 万首**歌曲的向量检索 + **6277 个标签**的语义中间层
- 实现完整的**画像 → 推荐 → 反馈 → 画像更新**闭环
- 完成系统性的**性能优化**（反馈页 3s → 6ms）与**稳定性加固**

**技术亮点**

- 画像独立化：从"算法附属"到"共享资产"
- RRF + MMR：解决多路召回融合与结果多样性
- 三套提示词：让无种子场景的推荐理由不再"胡编"
- 降级兜底：LLM 不可用时系统仍可用

**后续方向**

- 协同过滤结果接入线上（ALS 已有离线实验）
- 引入更多隐式反馈信号（跳过、听完率）
- 召回通道扩展（`related_artist_idxs` 之外的音乐图谱关系）
- 画像在线更新（当前为反馈触发重建）

> **讲稿**：最后总结一下。这个系统最有价值的地方不是用了多先进的模型，而是**把工程问题拆解清楚**：用户有三种需求，就用三条路径；画像要共享，就独立成层；多路召回量纲不可比，就用 RRF；模型可能失败，就设计降级。谢谢大家。

---

# 附：演示环境与运行说明

**技术依赖（均已实测运行）**

| 组件 | 地址 | 说明 |
|---|---|---|
| Django 服务 | `http://127.0.0.1:8000` | 主应用 |
| Qdrant | `http://127.0.0.1:6333` | 向量库（Docker/WSL 部署） |
| Ollama | `http://127.0.0.1:11434` | 本地大模型 |

**启动命令**
```bash
python manage.py migrate            # 初始化数据库
python manage.py build_alias_cache  # 生成画像别名缓存（首次）
python manage.py runserver          # 启动服务
```

**故障恢复**：`ollama_doctor.bat` —— 检测并修复 Ollama 服务状态

**代码规模**

| 模块 | 文件数 | 行数 | 职责 |
|---|---|---|---|
| 推荐引擎 | 4 | ~1,400 | 三路线 + 融合重排 |
| 画像服务 | 1 | 718 | 画像读写与向量化 |
| 检索层 | 3 | ~620 | 声学相似 / 标签 / 歌曲过滤 |
| API 层 | 1 | 513 | 15 个路由端点 |
| 前端 | 5 | ~3,050 | 单页应用 + 4 个功能页 |




