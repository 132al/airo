# SQLite 写锁问题 · 诊断与修复

## 一、问题现象

真实并发压测（多用户同时使用）时发现：

```
HTTP 400 {"error": "database is locked"}
```

| 并发用户 | 请求成功 | 请求失败 | **失败率** |
|---|---|---|---|
| 4 | 5 | 7 | **58%** |
| 8 | 6 | 18 | **75%** |
| 12 | 10 | 38 | **79%** |

**复现条件**：多个用户**同时点赞**（`POST /api/feedback/`）。
只读接口（推荐、搜索、浏览）不受影响。

---

## 二、定位过程

### 第 1 步：确认不是 Qdrant

先对 Qdrant 做专项并发测试（覆盖系统实际使用的 4 种查询）：

| 查询类型 | 并发 8 | 并发 16 | 并发 32 | **并发 64** |
|---|---|---|---|---|
| 向量检索（200条+向量） | 0 err | 0 err | 0 err | **0 err** |
| 流派过滤（300条） | 0 err | 0 err | 0 err | **0 err** |
| 艺术家索引（+向量） | 0 err | 0 err | 0 err | **0 err** |
| 文本匹配 | 0 err | 0 err | 0 err | **0 err** |

**Qdrant 在 64 并发下零错误，吞吐 200~570 QPS** → 排除。

### 第 2 步：捕获真实错误

拦截 ORM 的 SQL 执行，抓到失败语句：

```
SQLFAIL x15  INSERT INTO "user_feedback" (...)
```

**15 次失败全部是同一条 INSERT** → 锁定为 SQLite 写锁。

### 第 3 步：用环境变量/配置试错，证伪 WAL 方案

| 方案 | `journal_mode` | `busy_timeout` | 并发 8 失败率 |
|---|---|---|---|
| 现状（不改） | delete | 5000 | 62% |
| WAL + timeout 20 | wal | 20000 | **78%** |
| WAL + timeout 60 | wal | 60000 | **66%** |
| WAL + sync=NORMAL | wal | 20000 | **72%** |

**结论：WAL 和 busy_timeout 完全无效。** 这是我一开始推荐的方案，被实测证伪。

### 第 4 步：找到真正的根因（换事务模式）

只改事务模式，其它不变：

| 事务模式 | 并发 12 结果 |
|---|---|
| `BEGIN`（deferred，**Django 默认**） | **0 成功 / 48 失败（100% 失败）** |
| **`BEGIN IMMEDIATE`** | **24 成功**，锁冲突消失 ✅ |

---

## 三、根因

### 为什么 `busy_timeout` 救不了

Django 默认用 `BEGIN`（deferred）开事务：

```
1. BEGIN         → 不取任何锁
2. SELECT        → 取读锁 (SHARED)         ← update_or_create 的第一步
3. INSERT/UPDATE → 需要升级为写锁 (RESERVED)
4. 若别人已持写锁 → 锁升级失败
```

> **关键：锁升级失败时 SQLite 立即返回 `SQLITE_BUSY`，不等待、无视 `busy_timeout`。**

这是 SQLite 的**官方设计** —— 锁升级有死锁风险，必须立即失败让应用层决定怎么办。

而 Django 的 `update_or_create()` 恰好是"先 SELECT 再 INSERT"的读-改-写模式，
**必然踩这个坑**。这也解释了两个反直觉现象：
- 为什么 `timeout` 设到 60 秒也没用
- 为什么 WAL 也救不了（WAL 解决读写冲突，不解决写-写升级冲突）

`BEGIN IMMEDIATE` 有效是因为它**在事务开始时就取写锁**，于是 `busy_timeout` 能正常等待。
但改事务模式要动 Django 内部（自定义 CursorWrapper），侵入性大。

---

## 四、修复方案：应用层重试

锁冲突是**瞬时**的（持锁方通常几毫秒内提交），所以"捕获后小睡再重试"既简单又有效。

### 新增 `webtest/db_retry.py`

```python
def is_lock_error(exc):
    """判断是否为 SQLite 写锁冲突"""
    msg = str(exc).lower()
    return "database is locked" in msg or "database table is locked" in msg


def with_retry(func=None, *, attempts=12, base_delay=0.05, max_delay=1.0):
    """装饰器：锁冲突时指数退避重试（含宽抖动）"""

def retry_call(func, *args, **kwargs):
    """函数式调用版"""
```

**三个设计要点**：

| 要点 | 说明 |
|---|---|
| **只重试锁定错误** | 其它异常（唯一约束、缺表）原样抛出，不掩盖真 bug |
| **指数退避 + 宽抖动** | 基准 0.05→0.1→...→1.0s，再乘 `uniform(0.5, 2.0)` 抖动 |
| **attempts=12** | 实测 8 次在 24 人并发下仍有 1.7% 饿死；提到 12 次 + 宽抖动后归零 |


### 应用到 3 处写操作

| 文件 | 位置 | 说明 |
|---|---|---|
| `webtest/views.py` | `feedback_api` | 把"写反馈 + 回写画像"整体包进一个 `_write()` 重试 |
| `webtest/playlist_import.py` | `import_playlist` | 导入写反馈 + `rebuild_profile` 都走重试 |
| `webtest/profile_service.py` | `save()` | `get_or_create + save` 是读-改-写，同样包重试 |

**注意**：`rebuild_profile()` 也会写 `UserProfile`，所以必须**一起**放进重试范围，
否则反馈写成功但画像写失败，仍然报错。

---

## 五、修复效果

### 直接压测 `/api/feedback/`

| 并发 | 修复前失败率 | **修复后失败率** |
|---|---|---|
| 4 | 58% | **0%** ✅ |
| 8 | 75% | **0%** ✅ |
| 12 | 79% | **0%** ✅ |

### 真实场景回归（多用户走完整流程：推荐→找相似→补链→点赞→看反馈页）

| 用户数 | 点赞成功 | **点赞失败** | 全接口错误 | QPS |
|---|---|---|---|---|
| 1 | 3 | **0** | **0** | 2.96 |
| 2 | 6 | **0** | **0** | 10.03 |
| 3 | 9 | **0** | **0** | 11.87 |
| 6 | 12 | **0** | **0** | 13.25 |

**6 用户并发时，60 个请求零错误。**

---

## 五之二、24 人压测（最终验收）

### 完整流程（每人 2 轮：推荐→找相似→补链→点赞→反馈页）

| 指标 | 第 1 轮 | 第 2 轮 | 第 3 轮 |
|---|---|---|---|
| 用户数 | 24/24 响应 | 24/24 | 24/24 |
| 总请求 | 240 | 240 | 240 |
| **错误数** | **0** | **0** | **0** |
| **点赞成功/失败** | **48 / 0** | **48 / 0** | **48 / 0** |
| QPS | 13.65 | 28.05 | 32.90 |
| 用户耗时（p50） | 13.4s | 4.5s | 4.0s |
| 用户耗时（max） | 15.1s | 7.2s | 6.2s |

### 各接口延迟（24 人并发，取代表性一轮）

| 接口 | 次数 | P50 | P95 | 最大 | 错误 |
|---|---|---|---|---|---|
| 每日推荐 | 48 | 393ms | 1625ms | 2932ms | **0** |
| 歌名找相似 | 48 | 200ms | 319ms | 355ms | **0** |
| 按需补链 | 48 | 1141ms | 4115ms | 4435ms | **0** |
| 点赞 | 48 | 180ms | 1337ms | 2134ms | **0** |
| 反馈页 | 48 | 52ms | 409ms | 486ms | **0** |

### 专项：24 人纯点赞（每人 10 次 = 240 次写）

调参过程与结果：

| 参数 | 抖动 | 失败数 | 失败率 |
|---|---|---|---|
| `attempts=8` | `[0, +50%]` | 4 | **1.7%** ← 仍有饿死 |
| **`attempts=12`** | **`[50%, 200%]`** | **0** | **0%** ✅ |

**连续 5 轮验证**：

```
Round 1 : total=240 ok=240 fail=0  rate=0.0%
Round 2 : total=240 ok=240 fail=0  rate=0.0%
Round 3 : total=240 ok=240 fail=0  rate=0.0%
Round 4 : total=240 ok=240 fail=0  rate=0.0%
Round 5 : total=240 ok=240 fail=0  rate=0.0%
```

**5 轮共 1200 次并发写，零失败。**

### 为什么要"宽抖动"

24 个连接几乎同时抢锁。若退避时间接近，它们会**反复在同一时刻重撞**（类似 CSMA 碰撞）。
把抖动从 `[0, +50%]` 放宽到 `[50%, 200%]` 后，各请求的等待被打散，冲突概率显著下降。

这也是 `attempts=8 → 12` 效果提升的真正原因 —— **不是等得更久，而是撞得更散**。

---

## 五之三、内存与稳定性（24 人压测全程）

| 时间点 | RSS | 线程 | 句柄 | 系统可用内存 |
|---|---|---|---|---|
| 压测前 | 133 MB | 3 | 226 | 3.97 GB |
| 24人×1轮 | 135 MB | 5 | 222 | 4.33 GB |
| +3轮（720 请求） | 139~141 MB | 5 | 235~241 | 4.61~4.71 GB |
| **累计 3000+ 请求** | **145 MB** | **5** | **219** | **4.33 GB** |

> **3000+ 请求后 RSS 仅从 133MB 涨到 145MB（+12MB），句柄反而下降。**
> **无泄漏、无崩溃、服务持续 HTTP 200。**


### 各接口延迟（6 用户并发）

| 接口 | P50 | 最大 | 错误 |
|---|---|---|---|
| 每日推荐 | 379ms | 538ms | 0 |
| 歌名找相似 | 149ms | 202ms | 0 |
| 按需补链 | 1218ms | 1621ms | 0 |
| 点赞 | 31ms | 201ms | 0 |
| 反馈页 | 20ms | 118ms | 0 |

注：补链 P50 1218ms 是因为并发时 Meting 全局锁串行 + 缓存未命中，属预期行为（不是本次问题）。

---

## 六、验证清单

| 测试 | 结果 |
|---|---|
| `manage.py check` | 0 issues |
| **`test_db_retry.py`（新增）** | **17 / 17 通过** |
| `test_calibration.py` | 27 / 27 通过 |
| `test_playlist_import.py` | 40 / 40 通过（0 失败） |
| 6 用户并发完整流程 | **零错误** |
| **24 用户并发完整流程（3 轮 720 请求）** | **零错误** |
| **24 用户纯点赞（5 轮 1200 次写）** | **零失败** |
| 累计 3000+ 请求后内存 | 133MB → 145MB（**无泄漏**） |
| 服务健康 | 全程 HTTP 200 |

### 新增测试 `test_db_retry.py` 覆盖

- **锁错误识别**：正确识别 + 不误判（`no such table` / 唯一约束）
- **重试行为**：中途成功、耗尽后抛原异常、非锁错误不重试、`retry_call` 传参
- **并发写（核心）**：并发 **4 / 8 / 24** 写操作 **零锁失败**
- **数据完整性**：反馈记录与画像均成功落库且非空


---

## 七、遗留问题（如实列出）

| 问题 | 说明 |
|---|---|
| **SQLite 单写者本质未变** | 重试只缓解冲突，不改变"同一时刻一个写连接"。极高并发下仍会排队（符合"排队可接受"的标准），但不再有 50%~79% 失败率 |
| **写吞吐有上限** | 并发写会被串行化 + 重试等待。实测 24 人 240 次写需约 11s（≈ 22 写/秒） |
| **根治需换 PostgreSQL** | 真正的 MVCC 并发，无单写者限制。代价：装服务 + 迁移数据 |
| **重试会掩盖慢查询** | 若某次写操作本身很慢（长时间持锁），其他请求会重试到预算上限后仍失败。当前 24 人压测未观察到 |
| **只覆盖了已知写点** | 本次修了 3 处（`feedback_api` / `playlist_import` / `profile_service.save`）。若将来新增写操作，需同样包重试 |
| `busy_timeout` 保持默认 | 因为实测无效（Django 的 deferred 模式下不生效），所以**没有**改 `settings.py` 的 `OPTIONS` —— 避免给出"看起来修了但实际没用"的假配置 |
| **重试预算约 7 秒** | `attempts=12` + 宽抖动，最坏情况总等待约 6~7 秒。点赞是低频操作，用户可接受；但若压到更高并发，仍可能耗尽 |

---

## 八、结论

> **问题：** SQLite 单写者 + Django `BEGIN(deferred)` 模式下 `busy_timeout` 不生效，
> 多用户同时点赞导致 58%~79% 请求失败。
>
> **根因：** 锁升级失败时 SQLite 立即报错、不等待（官方设计）。
> WAL 和 timeout 都无效 —— 已实测证伪。
>
> **修复：** 应用层指数退避重试（`webtest/db_retry.py`），
> 关键是**宽抖动**（`[50%, 200%]`）—— 不是等得更久，而是撞得更散。
>
> **效果：**
> - 失败率 **58%~79% → 0%**
> - **24 人并发完整流程**：3 轮 720 请求**零错误**
> - **24 人纯点赞**：5 轮 1200 次写**零失败**
> - 累计 **3000+ 请求内存仅 +12MB**，无泄漏、无崩溃
> - 回归测试 **17/17 通过**


