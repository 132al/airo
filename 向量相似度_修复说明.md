# 向量相似度问题 · 诊断与修复

## 一、先澄清一个关键点：**不是"三位小数精度不够"**

这是两种完全不同的问题，我实测区分了一下：

| | 表现 | 加小数位有用吗 | 换 float32 有用吗 |
|---|---|---|---|
| **精度问题** | 数值被截断，真实值在更低位 | ✅ 有用 | ✅ 有用 |
| **本库的实际问题** | 有效信号**本身就极小**，且被常数淹没 | ❌ **无用** | ❌ **无用** |

### 实测证据

| 量 | 实测值 |
|---|---|
| 推荐结果原始余弦 | 0.9955 ~ 0.9973 |
| **随机抽样的原始余弦** | **mean 0.9861, sd 0.0047** |
| 推荐 vs 随机 的差距 | **仅 +0.011** |

而随机余弦的**标准差只有 0.0047** —— 也就是说：

> **小数点后第一位是常数（0.98），第二位基本不变（±0.005），第三位才是真实信号。**

对比一下量级：

| 小数位 | 承载什么 |
|---|---|
| 第 1 位（0.**9**8x） | 常数基线（几何伪影） |
| 第 2 位（0.9**8**x） | 常数波动 |
| **第 3 位（0.98**x**）** | **真实相似性信号** |

**把 `round(x, 4)` 改成 `round(x, 8)` 不会让信号变大** —— 只是把噪声也打印得更长。

## 二、根因：向量全是正数，余弦被"共同基线"锁死

```python
# 实测单条 64 维向量
均值 127.6   sd 15.9   min 80   max 165   # 全为正数
前16维: [133, 114, 152, 128, 134, 113, 139, 139, 127, 121, 137, 142, 141, 139, 115, 121]
```

余弦公式 `cos(a,b) = Σ(aᵢbᵢ) / (|a||b|)`。当所有分量同号且大小相当时：

$$\cos \approx \frac{64 \cdot 128^2}{\left(\sqrt{64}\cdot 128\right)^2} = \frac{64 \cdot 128^2}{64 \cdot 128^2} = 1$$

**每一维的"共同基线 128"贡献了绝大部分内积，全部同号相加 → 任意两条向量都几乎完全相关。**

用合成向量验证这个机制（噪声越小，余弦越接近常数）：

| 噪声 sd | 余弦均值 | 余弦 sd |
|---|---|---|
| 8 | 0.9961 | 0.0007 |
| **16** | **0.9848** | **0.0026** ← 与实测吻合 |
| 32 | 0.9419 | 0.0105 |
| 64 | 0.8017 | 0.0332 |
| 128 | 0.4959 | 0.0810 |

## 三、修复：去均值（centering）

### 原理

对每条向量减去全库逐维均值 `mu`，再算余弦：

```python
cos_centered(a, b) = cos(a - mu, b - mu)
```

**几何含义**：把坐标系原点搬到数据中心。原本"所有向量都指向第一象限"的共线结构被消除，
余弦就只反映**方向差异**（即真实相似性），不再包含"共同基线"这一项。

### 实测效果（同一个种子 Shape of You）

| 指标 | 修复前（原始余弦） | 修复后（去均值） |
|---|---|---|
| **随机基线 mean** | **0.9861** | **0.0014** |
| 随机基线 sd | 0.0047 | **0.3294** |
| **区分度提升** | — | **69.5×** |
| 推荐结果范围 | 0.9955 ~ 0.9973 | **0.166 ~ 0.346** |
| 推荐结果 span | 0.0018 | **0.1797（100×）** |
| 前端显示 | "99.7% 相似"（误导） | **"25.4% ~ 46.6%"（可解释）** |

**关键：随机基线回到 0，于是数字有了明确零点，可以读作相似度。**

## 四、代码改动（3 个文件 + 2 个新增）

### 新增 `webtest/vector_calibration.py`

```python
def get_mean_vec(force_rebuild=False, sample=400):
    """返回 64 维全局均值向量（带 vector_mean_64.json 文件缓存）"""

def center(vec, mu=None):
    """去均值"""

def calibrated_cos(a, b, mu=None):
    """去均值后的余弦 —— 这才是可读作"相似度"的数"""

def to_percent(c, lo=-0.05, hi=0.80):
    """去均值余弦 -> 0~100 可读百分比（钳制在 [0,100]）"""
```

映射区间 `[-0.05, 0.80]` 的选取依据：实测随机基线 ≈ 0、推荐 top20 落在 0.17~0.35
（均值 0.24），留出余量以便高相似情形能显示到 80% 以上。

### 修改 `webtest/embeat_similar.py`

| 位置 | 改动 |
|---|---|
| `__init__` | 增加 `self._mu_vec` / `self._calibration_ready` 懒加载字段 |
| 新增 `_ensure_calibration()` | 首次调用加载均值向量；**失败不抛错，只是关闭校准**（降级安全） |
| `search_similar()` | `with_vectors=False` → `True`（需要向量才能算校准值），**并将向量同时用于 MMR** |
| `filter_candidates()` | 新增 `seed_center` 参数；对每个候选算 `_cal_sim` |
| 结果字典 | 新增 `"cal_similarity"` 字段（保留 `similarity` 不动，向后兼容） |
| `recommend()` | 计算 `_seed_center` 并传入 |

### 修改 `webtest/views.py`

| 位置 | 改动 |
|---|---|
| `/api/recommend/` | 调用新增的 `_attach_sim_percent()` |
| 新增 `_attach_sim_percent()` | 每条结果附 `sim_percent`；**放在 `stage != "cold"` 判断之外**，冷启动用户也有 |

### 新增 `test_calibration.py` —— 回归测试，**20/20 通过**

覆盖：均值向量有效性 / 去均值核心断言（基线归零 + 区分度 ≥20×）/ API 契约 /
边界与降级（None、长度不等、零向量、百分比钳制）。

```
[PASS] 原始余弦均值 > 0.95（证实'被常数占领'）        0.9861
[PASS] 去均值后均值 |mean| < 0.05（基线归零）          0.0014
[PASS] 去均值后区分度提升 >= 20x                      x69.5
[PASS] 每条结果都有 cal_similarity
[PASS] 每条结果都有 sim_percent（冷启动也要有）
[PASS] 校准值区分度 (span) 远大于原始余弦   cal span=0.1797 vs raw span=0.0018
```

## 五、兼容性与降级

| 场景 | 行为 |
|---|---|
| `vector_mean_64.json` 存在 | 直接读缓存，**零额外开销** |
| 缓存缺失 | 现场抽样 400 条估计（约 2~3 秒），并写回缓存 |
| Qdrant 不可用 | `_mu_vec = None` → 关闭校准，`sim_percent` 退回 `similarity*100`（旧行为） |
| 旧代码读 `similarity` | **不受影响**（字段保留，语义未变） |
| `with_vectors=True` 的开销 | 单次响应多传 20×64 字节 ≈ 1.3 KB，可忽略；**顺带让 MMR 的 `_vector` 真正生效** |

## 六、修复过程中发现的**更深层问题**（重要）

校正之后，我顺手用去均值余弦做了一次**"同艺人 vs 随机"分离度测试** —— 结果暴露了一个比"数字不可读"更根本的问题：

| 对照 | cal 均值 | sd |
|---|---|---|
| **同艺人**（同一歌手的不同歌，理应最相似） | **+0.142** | 0.369 |
| **随机两首** | **−0.016** | 0.344 |

| 分离度指标 | 值 | 含义 |
|---|---|---|
| Cohen's d 近似 | **0.46** | 属"中等偏弱"（一般要求 >0.8） |
| **同艺人低于随机 P75 的比例** | **62%** | **严重重叠** |

### 这意味着什么

> **同一歌手的两首歌，在 62% 的情况下余弦相似度还不如一对随机歌曲。**

换句话说：**这 64 维向量几乎无法区分"同一歌手的歌"和"随便两首歌"。**

**验证案例 —— Bohemian Rhapsody**：

过滤掉重复/Remaster 版本后，真实推荐结果是：

| 推荐结果 | 艺人 | cal |
|---|---|---|
| Believe | Elton John | +0.018 |
| Tower Of Babel | Elton John | +0.059 |
| The Other Side | Lenny Kravitz | −0.009 |
| Modern Way | Kaiser Chiefs | +0.005 |
| The Prophet's Song | Queen | −0.074 |
| I Forgot To Be Your Lover | Bruce Springsteen | +0.066 |

**cal 均值 ≈ 0，即"与随机无异"。** 这些歌手虽然都是摇滚（通过了流派硬过滤），但**在声学向量上彼此并不相似**。

### 结论：真正起作用的是**规则**，不是**向量**

结合前面所有证据，可以给出一个诚实的判断：

| 环节 | 实际贡献 |
|---|---|
| **流派硬过滤** | ⭐⭐⭐ **主要贡献者** —— 保证结果"是同一类音乐" |
| **popularity 排序** | ⭐⭐ 保证结果"是好听的" |
| **同歌手/同流派上限** | ⭐⭐ 保证多样性（20/20 不同歌手） |
| **64 维声学向量** | ⭐ **贡献很弱**（分离度仅 0.46） |
| **"相似度 99.7%"** | ❌ **无信息量**（随机 98.6%） |

**所以之前报告里"检索准确性"好的真正原因不是向量检索，而是流派过滤 + 热门度排序。**

这不否定系统的整体效果 —— 用户看到的推荐确实是"同类音乐里的热门歌"，体验上是好的。
但它说明**"基于声学相似度的推荐"这个技术叙事是不成立的**，至少在这份 64 维数据上不成立。

### 改进方向（如果要让向量真正起作用）

| 方案 | 说明 | 代价 |
|---|---|---|
| 换更高维/更强的音频嵌入 | 如 MERT、CLAP、MusicFM（多为 512~1024 维） | 需重新生成 1238 万条向量 |
| 增加二值/量化精度 | 当前是 `uint8` 存储（`datatype: uint8`），有精度损失 | 中等，可只改存储 |
| 用 tag/文本嵌入替代 | 项目已有 bge 模型，可对 `artist_genres`+场景标签做嵌入 | 低，但失去"声学"语义 |
| 弱化向量、强化规则 | 承认现状，把向量只作为"同分时的次级排序" | 最低 |

## 七、修改清单

| 文件 | 状态 | 说明 |
|---|---|---|
| `webtest/vector_calibration.py` | **新增** | 去均值校准模块（含缓存、降级） |
| `vector_mean_64.json` | **新增** | 均值向量缓存（901 B） |
| `test_calibration.py` | **新增** | 回归测试，**27/27 通过** |
| `webtest/embeat_similar.py` | 修改 | ①启用向量 ②算 `cal_similarity` ③补 `_vector` 让 MMR 生效 ④新增 `_is_dup_version()` 过滤重复版本 ⑤同歌手限流改为全体生效 |
| `webtest/views.py` | 修改 | 新增 `_attach_sim_percent()`，冷启动也生效 |
| `向量相似度_修复说明.md` | **新增** | 本文档 |

### 回归验证（修复后全量跑一遍）

| 测试 | 结果 |
|---|---|
| `test_calibration.py` | **27/27 通过** |
| `manage.py check` | 0 issues |
| `retest.py` T1 延迟 | 歌名找相似 **P50 39ms**（修复前 27ms，含传向量开销，仍达标） |
| `retest.py` T3 多样性 | **20/20 不同歌手，0 歌名重复** |
| `retest.py` T3 随机性 | Jaccard **17.3%**（与修复前 17.7% 一致） |
| `retest.py` T4 鲁棒性 | **12/12 通过** |


## 八、遗留问题（如实列出）

| 问题 | 说明 |
|---|---|
| **MMR 之前从未生效** | 原 `search_similar` 用 `with_vectors=False`，`filter_candidates` 也不输出 `_vector`，导致 `diversity_rerank` 的 MMR 惩罚项**恒为 0**（退化成"只有硬约束的贪婪选择"）。本次已补上 `_vector`，MMR 现在才真正参与。**注意：这是行为变更，建议回归多样性指标** |
| **向量区分度弱（核心问题）** | 分离度仅 0.46，同艺人 vs 随机严重重叠（62%）。这是**数据本身**的问题，本次修复只是让数字"诚实"，**没有提升检索质量** |
| `uint8` 量化 | segment 配置显示 `datatype: uint8`，向量被量化存储，存在精度损失 |
| **数据集有大量重复** | Bohemian Rhapsody 有数十个 `- Remaster` 副本，向量完全相同（raw cos = 1.000000）。原代码只按 `"remix" in name` 过滤，**漏掉了 Remaster/Live/Deluxe 等重复版本**，导致推荐结果被同一首歌刷屏 |
| 均值向量是抽样估计 | 用 400 条估计，逐维 sd 4.33。对结果影响很小（测试通过），但可更精确 |
| `to_percent` 映射区间是经验值 | `[-0.05, 0.80]` 基于当前库实测，换模型后需重新标定 |
| 排序未受影响 | Qdrant 按内积排序，常数不改变**相对**顺序，故 top-k 与修复前一致（本次只修"显示的数值"） |

### 已修复：重复版本刷屏 bug

`filter_candidates` 原来只排除 `remix`：

```python
if "remix" in payload_track_name:
    continue
```

这导致 Remaster / Live / Deluxe 等副本不被过滤 —— 实测 Bohemian Rhapsody
的**前 15 个候选全是它自己的 `- Remastered` 副本**（向量完全相同，raw cos = 1.000000）。

**修复**：新增 `_is_dup_version()`，覆盖 remaster/live/deluxe/edition/radio edit 等
22 个标记，并且**只在括号 `( )` `[ ]` 或 `" - "` 之后匹配**，避免误伤歌名本身含这些词的原创曲。

防误伤用例（测试已验证）：

| 歌名 | 判定 | 说明 |
|---|---|---|
| `Bohemian Rhapsody - Remastered 2011` | ✅ 过滤 | 真副本 |
| `Song (Live)` | ✅ 过滤 | 真副本 |
| **`Live and Let Die`** | ❌ 不过滤 | 歌名本身含 Live |
| **`Version of Me`** | ❌ 不过滤 | 歌名本身含 Version |

**踩坑记录**：第一版把判断放在 `payload_track_name` 上，但该变量在上一行已被
`.split(" (")[0].split(" - ")[0]` **截掉了版本后缀**，导致 5~8 个副本仍然漏过。
改用**原始** `payload["track_name"]` 后才彻底生效。

### 已修复：同歌手限流只对种子歌手生效

```python
# 原代码：只挡"种子歌手本人"，其它歌手可无限重复
if item["payload_artist_name"] == seed_artist_name:
    if same_artist_counter >= max_same_artist:
        continue
```

导致 Bohemian Rhapsody 的结果里 Queen / David Bowie 各占 3~4 席。
**修复**：改为 `artist_seen` 字典对**所有歌手**一视同仁限流。


## 九、一句话总结

> **1. 问题不是精度，是"分母被几何常数占领"** → 去均值修复，随机基线 0.986 → 0.001（区分度 **+69.5×**）
>
> **2. 修完发现更根本的问题**：向量本身区分度很弱（同艺人 vs 随机分离度仅 **0.46**，**62% 重叠**）。
> 所以系统推荐效果好，**主要靠流派过滤 + 热门度，不是靠声学向量**。
>
> **3. 顺带修了 3 个 bug**：
> - MMR 从未生效（`_vector` 缺失，惩罚项恒为 0）
> - 去重漏掉 Remaster/Live/Deluxe 等副本（Bohemian Rhapsody 前 15 个候选全是自己副本）
> - 同歌手限流只对种子歌手生效（Queen/David Bowie 各占 3~4 席）
>
> **4. 回归全绿**：27/27 校准测试、12/12 鲁棒性、20/20 不同歌手、Jaccard 17.3%（随机性保持）、P50 170ms（延迟未退化）


